# ROBOX UI — Python package requirements
# Supports Python 3.9, 3.10, 3.11, 3.12
# pip automatically selects the correct pre-built wheel for the installed Python version.
# Environment markers below prevent pip from pulling incompatible versions on older Pythons.

pyserial>=3.5

# numpy 2.1+ requires Python 3.10+; cap at <2.1 for Python 3.9 users
numpy>=1.24,<2.1; python_version == "3.9"
numpy>=1.24; python_version >= "3.10"

# scipy 1.13+ requires Python 3.10+; cap at <1.13 for Python 3.9 users
scipy>=1.10,<1.13; python_version == "3.9"
scipy>=1.10; python_version >= "3.10"

matplotlib>=3.7
dynamixel-sdk>=3.8
keyboard>=0.13
PyQt5>=5.15.9
PyQtWebEngine>=5.15.5
pymupdf>=1.23
markdown>=3.4
pymongo>=4.4
dnspython>=2.3
cffi>=1.15
bcrypt>=4.0
cryptography>=40
requests>=2.28
pynput>=1.7
pywin32>=305; sys_platform == "win32"
winshell>=0.6; sys_platform == "win32"

# mujoco 3.1.6 ships pre-built wheels for Python 3.9, 3.10, 3.11, 3.12
mujoco==3.1.6